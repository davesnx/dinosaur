// import { ACTIVE_QUESTION, ANSWER_QUESTION, LOAD_FORM } from './../constants/actionTypes'
import { RESTART_GAME, JUMP } from 'constants/actionTypes'

const initialState = {
  distance: 0,
  isDeath: false
}

function gameReducer (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default gameReducer
