module Constants = {
  let gravity = 2000.0;
  let floorHeight = 32;
  let flapForce = (-250.0);

  /* Speed - horizontal speed of the flappy bird */
  let speed = 200.;

  /* width - the width of our 'game surface' */
  let width = 600;
  let height = 300;

  let birdX = 50;
  let pipeGap = 200;
};
